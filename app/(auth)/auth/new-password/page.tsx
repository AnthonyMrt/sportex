import React from "react";
import NewPasswordForm from "@/components/auth/new-password-form";

const page = () => {
  return (
    <main>
      <NewPasswordForm />
    </main>
  );
};

export default page;
