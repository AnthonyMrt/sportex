import LoginForm from "@/components/auth/login-form";
import React from "react";

type Props = {};

const page = ({}: Props) => {
  return (
    <main>
      <LoginForm />
    </main>
  );
};

export default page;
